{
  "name": "electron-shell",
  "version": "1.0.0",
  "private": true,
  "description": "Electron shell that loads the AI Terminal web app and exposes a secure native folder-picker bridge.",
  "main": "src/main.js",
  "scripts": {
    "postinstall": "electron-rebuild -f -w node-pty",
    "start": "electron ."
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.1",
    "electron": "^33.2.0"
  }
}
